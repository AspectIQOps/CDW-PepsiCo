docker compose up --build

### Final Checklist for Execution:

1.  **AWS Credentials:** The environment where you run the command **must** have valid AWS credentials (via an EC2 IAM role or local AWS config) that grant read access to your SSM path (`/aspectiq/demo`).
2.  **File Structure:** Ensure your local directory looks like this:

├── docker-compose.yaml
├── requirements.txt
├── docker/
│   ├── etl/
│   │   ├── Dockerfile
│   │   └── entrypoint.sh
│   └── db/
│       └── (empty or delete files here)
├── scripts/
│   └── etl/
│       ├── etl_utils.py
│       ├── appd_etl.py
│       └── snow_etl.py
└── sql/
    └── init/
        └── 02_create_tables.sql  <-- THIS IS THE NEW FILE

If this structure is correct, you are good to go! **Let me know if you run into any issues during the build or the ETL execution.**